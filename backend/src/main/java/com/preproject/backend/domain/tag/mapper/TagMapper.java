package com.preproject.backend.domain.tag.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface TagMapper {
    // TODO List<TagDto.Information> 써서 정보들 입력하는 방식으로 짤 예정
}
